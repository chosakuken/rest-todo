{
  "scripts": {
    "fix": "prettier -w .",
    "lint": "eslint src/**/*.js && prettier --check .",
    "build": "esbuild --bundle --platform=node --outdir=dist --packages=external src/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.0",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "prettier": "^3.8.3"
  }
}
